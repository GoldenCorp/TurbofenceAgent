services:
  fastapi_mcp:
    build:
      context: .
      dockerfile: Dockerfile
    # image: python:3.14-rc
    # container_name: amcp
    ports:
      - "9099:9099"
    # environment:
    #   - APP_ENV=production
    # volumes:
    #   -./app:/app
    # depends_on:
    #   - db
    networks:
      - my - network
#   db:
#     image: postgres:14
#     container_name: postgres - container
#     environment:
#       - POSTGRES_PASSWORD=eMHk1x6wDb81PiHw
#       - POSTGRES_USER=postgres
#       - POSTGRES_DB=postgres
#     volumes:
#       - postgres - data:/var/lib/postgresql/data
#     networks:
#       - my - network

# volumes:
#   postgres - data:

networks:
  my - network: